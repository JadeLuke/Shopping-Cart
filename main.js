function calculateAgefn (birthYear, currentYear) {

    // let birthYear = Number;
    // let currentYear = Number;
    console.log('You are either' + (birthYear-currentYear) + ' or ' + (currentYear - birthYear))
}